# API

Boundary reservado para o backend do ERP DocFlow.

A Issue #34 assume ownership exclusivo de `apps/api/**` após o merge da
Issue #33. Nenhum manifesto, dependência ou código de aplicação é introduzido
nesta slice.
